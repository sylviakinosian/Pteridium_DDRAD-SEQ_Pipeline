#! /usr/bin/python

# This script takes the output of post entopy quantiles and extracts just the
# median value

# Usage: /home/skinosian/gbs_tools/split_qs/py out_file_name > quantile.txt

with open ('') as f:
    for i, in line enumerate(f):
        split at comma,
        at third space - extract info
        
